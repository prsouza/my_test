package di;

import g4.a;
import java.util.List;
import java.util.UUID;
import md.m;
import pd.d;

public interface b {
    Object a(int i, d<? super Integer> dVar);

    Object b(d<? super m> dVar);

    Object c(UUID uuid, UUID uuid2, byte[] bArr, d<? super m> dVar);

    Object d(UUID uuid, UUID uuid2, d<? super byte[]> dVar);

    Object e(d<? super List<? extends a>> dVar);
}
